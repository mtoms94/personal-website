---
import { getEntry, render } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const intro = await getEntry('intro', 'intro');
if (!intro) throw new Error('Intro content not found');

const { Content } = await render(intro);

/* Blog preview temporarily hidden — restore with getCollection when ready
import { getCollection } from 'astro:content';
const recentPosts = (await getCollection('blog'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);
*/
---

<BaseLayout
  title="Mairead Toms"
  description="My name is Mairead and sometimes Mai. I'm a software developer currently based in Virginia. Outside of work I enjoy biking, running, and finding different ways to hang out outside."
>
  <article class="page-intro">
    <h1>{intro.data.title}</h1>
    <Content />
  </article>

  {/*
  {recentPosts.length > 0 && (
    <section>
      <div class="section-heading">
        <h2>Recent posts</h2>
        <a class="text-link" href="/blog/">View all →</a>
      </div>
      <ul class="post-list">
        {recentPosts.map((post) => (
          <li>
            <a href={`/blog/${post.id}/`}>
              <span class="post-title">{post.data.title}</span>
              <span class="post-description">{post.data.description}</span>
            </a>
          </li>
        ))}
      </ul>
    </section>
  )}
  */}
</BaseLayout>
